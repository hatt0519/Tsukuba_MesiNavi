$("#now_open_shop").html('<%= j(render :partial => "now_open_shops", :locals => {:shops => @shops, :shops_unknown => @shops_unknown, :week => @week}) %>');
$("#loading").modal('hide');
$(function(){
  showShopCandidateLocation();
});
